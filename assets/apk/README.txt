Put your Android APK file here as:

  bet369win.apk

Then users can download via:
  /download-apk
  /app.apk

Optional: set APK_DOWNLOAD_URL in core/.env to an external full URL
(e.g. Google Drive / CDN). External URL takes priority over the local file.
